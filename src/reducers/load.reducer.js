const defaultState = {
  loaded: false,
  loadProgress: 0,
  loadMessage: "Spawning a new dimension..",
};

export const reducer = function (state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
